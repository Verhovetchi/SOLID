using System;

namespace DIP
{
     class Program
     {
          static void Main(string[] args)
          {
               //Player player = new SimplePlayer();
               //player.getSalary();

               //Salary salary = new SalaryPound();
               //Player player = new ModernPlayer(salary);
               //player.getSalary();

               //IExchange exchange = new SalaryEuro();
               //Player player = new theMostModernPlayer(exchange);
               //player.getSalary();

               Console.ReadKey();
          }
     }
}
